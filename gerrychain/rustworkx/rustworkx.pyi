"""Placeholder stub for the compiled extension module.

To be replaced by the vendored upstream rustworkx.pyi. `types.ModuleType` resolves attribute
access as `Any`, which is all the smoke-test hook needs.
"""

from types import ModuleType

_gerrychain: ModuleType
